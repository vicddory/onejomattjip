# ================================================================================
# 📁 .streamlit/config.toml - Streamlit 테마 및 서버 설정
# ================================================================================
#
# 이 파일은 Streamlit 앱의 전체적인 테마(색상)를 설정합니다.
# styles.py의 CSS와 함께 일관된 디자인을 만들어냅니다.
#
# ================================================================================

[theme]
# === 기본 색상 ===
primaryColor = "#00695C"        # Deep Emerald - 버튼, 링크, 강조색
backgroundColor = "#FAFAFA"      # Off-White - 메인 배경
secondaryBackgroundColor = "#FFFFFF"  # 사이드바, 카드 배경
textColor = "#333333"            # 기본 텍스트 색상

# === 폰트 ===
font = "sans serif"              # 기본 폰트 (Noto Sans KR은 CSS에서 적용)

[server]
# === 서버 설정 ===
headless = true                  # 헤드리스 모드 (배포 시)
enableCORS = false               # CORS 비활성화
enableXsrfProtection = false     # [수정됨] XSRF 보호 비활성화 (CORS false일 때 같이 꺼야 경고 안 뜸)
port = 8501                      # 포트 지정 (선택사항, 기본값 8501)

[browser]
# === 브라우저 설정 ===
gatherUsageStats = false         # 사용 통계 수집 비활성화

[runner]
# === 실행 설정 ===
magicEnabled = true              # 매직 커맨드 활성화